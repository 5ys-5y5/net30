# NET30 storefront

This Vite app consumes the product-agnostic design system in `../docs/design-system` without modifying it. All NET30-specific copy, price data, batch options, routes, and economics live in `src/product-definition.ts`.

```bash
npm install
npm run dev
npm run build
```
